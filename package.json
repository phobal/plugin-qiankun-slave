{
  "name": "plugin-qiankun-slave",
  "version": "v0.0.18",
  "description": "plugin for qiankun slave",
  "authors": {
    "name": "phobal",
    "email": "phobal@126.com"
  },
  "repository": "plugin-qiankun-slave",
  "peerDependencies": {
    "umi": "2.x || ^2.9.0-0"
  },
  "main": "lib/index.js",
  "src": "src/index.ts",
  "scripts": {
    "start": "cross-env APP_ROOT=$PWD/example umi dev",
    "build": "father-build",
    "prepublishOnly": "npm run build && np --no-cleanup --yolo --no-publish"
  },
  "devDependencies": {
    "cross-env": "^6.0.3",
    "father-build": "^1.8.0",
    "np": "^5.0.3",
    "umi": "^3.1.3",
    "umi-plugin-types": "^0.3.2",
    "umi-types": "^0.4.0-beta.4"
  },
  "files": [
    "dist",
    "lib",
    "src",
    "ui"
  ],
  "license": "MIT",
  "dependencies": {
    "react-draggable": "^4.1.0"
  }
}
